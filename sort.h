#ifndef SORT_H
#define SORT_H
#include <stdio.h>
#include <stdlib.h>
void bubble_sort(int *array, size_t size);
void print_array(const int *array, size_t size);
void selection_sort(int *, size_t);
void quick_sort(int *, size_t);
void quicksort(int*, size_t, size_t, int*);
#endif /* SORT_H */
